dist: trusty

language: java

jdk:
  - oraclejdk8

node_js:
  - 11.4

cache:
  directories:
  - $HOME/.m2
  - ./frontend/mobile/node_modules/

before_script:
  - npm install --prefix frontend/mobile/

script:
  - mvn clean test
  - npm run build --prefix frontend/mobile/